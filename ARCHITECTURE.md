# ASRS Application Architecture & Flow

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                         WEB BROWSER                                  │
│  (User Interface - HTML/CSS/JavaScript)                             │
└──────────────────────────────┬──────────────────────────────────────┘
                               │
                    ┌──────────▼─────────────┐
                    │   FASTAPI ROUTER      │
                    │   (app/routers/)      │
                    └──────────┬────────────┘
         ┌──────────────┬──────────────┬──────────────┬──────────┐
         │              │              │              │          │
    ┌────▼────┐   ┌────▼────┐   ┌────▼────┐   ┌────▼────┐  ┌─▼─┐
    │  Auth   │   │Dashboard│   │ Reports │   │Visualize│  │QR │
    │ Routes  │   │ Routes  │   │ Routes  │   │ Routes  │  │Rts│
    └────┬────┘   └────┬────┘   └────┬────┘   └────┬────┘  └─┬─┘
         │              │              │              │         │
         └──────────────┴──────────────┴──────────────┴─────────┘
                        │
         ┌──────────────▼──────────────┐
         │   BACKEND SERVICES          │
         │ (backend/services/)         │
         └─┬──────────────┬────────────┘
           │              │
      ┌────▼──────┐  ┌───▼──────────┐
      │ Inference │  │  QR          │
      │ Processing│  │  Generation  │
      │           │  │              │
      │ • OCR     │  │ • qrcode lib │
      │ • Detection│ │ • reportlab  │
      │ • S3 Upload│ │ • PDF output │
      └───┬───────┘  └──────────────┘
          │
    ┌─────▼─────────────────────┐
    │   EXTERNAL SERVICES       │
    └─┬──────────┬──────────────┘
      │          │
  ┌───▼───┐  ┌──▼────┐  ┌──────────┐
  │AWS S3 │  │Google │  │ MySQL    │
  │(Images)  │Cloud  │  │(Database)│
  │       │  │Vision │  │          │
  │       │  │(OCR)  │  └──────────┘
  └───────┘  └───────┘
```

## Request Flow Diagram

### Login Flow
```
┌─────────────────┐
│  User Visits    │
│   /login        │
└────────┬────────┘
         │
    ┌────▼──────────────────┐
    │ Display Login Form     │
    │ (login.html)          │
    └────┬───────────────────┘
         │
    ┌────▼──────────────────┐
    │ User Submits Form     │
    │ POST /login           │
    └────┬───────────────────┘
         │
    ┌────▼──────────────────────┐
    │ Validate Credentials      │
    │ with bcrypt              │
    └────┬──────────┬───────────┘
         │          │
    ┌────▼──┐  ┌────▼───┐
    │Valid? │  │Invalid │
    │ YES   │  │ NO     │
    └────┬──┘  └────┬───┘
         │          │
    ┌────▼──────┐  ┌───▼─────────┐
    │Create     │  │Show Error   │
    │Session    │  │Message      │
    │Store User │  │(login.html) │
    └────┬──────┘  └─────────────┘
         │
    ┌────▼────────────┐
    │Redirect to      │
    │/dashboard (303) │
    └────────┬────────┘
             │
         ┌───▼──────┐
         │Dashboard │
         │Ready!    │
         └──────────┘
```

### QR Generation Flow
```
┌──────────────────────┐
│  User Enters VIN     │
│  1HGCV41JXMN109186  │
└──────────┬───────────┘
           │
       ┌───▼─────────────────┐
       │ Validate (17 chars) │
       └───┬──────────┬──────┘
           │          │
       ┌───▼──┐  ┌────▼───┐
       │Valid?│  │Invalid │
       │ YES  │  │ NO     │
       └───┬──┘  └────┬───┘
           │          │
    ┌──────▼──────┐  ┌──▼────────┐
    │Generate QR  │  │Show Error  │
    │Code         │  │Message     │
    │using qrcode │  │            │
    └──────┬──────┘  └────────────┘
           │
    ┌──────▼──────────────┐
    │Create PDF with      │
    │ReportLab            │
    │• QR Image           │
    │• VIN Label          │
    │• Timestamp          │
    │• ASRS Branding      │
    └──────┬──────────────┘
           │
    ┌──────▼──────────────┐
    │Save to /temp/       │
    │QR_{VIN}.pdf         │
    └──────┬──────────────┘
           │
    ┌──────▼──────────────┐
    │Return PDF File      │
    │Download Triggered   │
    └──────────────────────┘
```

### Image Upload & Processing Flow
```
┌──────────────────────┐
│  User Uploads Images │
│  Report Name + Files │
└──────────┬───────────┘
           │
       ┌───▼─────────────────────┐
       │ Create Report Record    │
       │ in Database             │
       │ return report_id        │
       └───┬────────────────────┘
           │
       ┌───▼──────────────────────────┐
       │ Save Files to Directory      │
       │ uploads/{report_name}_{uuid} │
       └───┬──────────────────────────┘
           │
       ┌───▼────────────────────────────────┐
       │ Queue Background Task              │
       │ get_inferences(report_dir, id)     │
       └───┬────────────────────────────────┘
           │
       ┌───▼──────────────────────┐
       │ For Each Image:          │
       │                          │
       │ 1. Process Image:        │
       │    • Call Google OCR     │
       │    • Parse Results       │
       │    • Detect Vehicle      │
       │                          │
       │ 2. Upload to S3          │
       │    • Save Image File     │
       │    • Get URL             │
       │                          │
       │ 3. Save to DB            │
       │    • Create Inference    │
       │    • Store Results       │
       │    • Link to Report      │
       └───┬──────────────────────┘
           │
       ┌───▼─────────────────────┐
       │ Processing Complete     │
       │ Images Now Visible in   │
       │ Visualization Page      │
       └─────────────────────────┘
```

## Database Schema

```
┌─────────────────────┐
│      users          │
├─────────────────────┤
│ id (PK)            │
│ username (Unique)  │
│ hashed_password    │
│ created_at         │
└─────────────────────┘

┌─────────────────────┐
│      reports        │
├─────────────────────┤
│ id (PK)            │
│ name               │
│ user_id (FK)       │
│ created_at         │
└─────────────────────┘

┌──────────────────────────┐
│     inferences           │
├──────────────────────────┤
│ id (PK)                 │
│ report_id (FK)          │
│ image_name              │
│ unique_id (From OCR)    │
│ quantity                │
│ vin_no                  │
│ exclusion               │
│ s3_obj_url (S3 Link)    │
│ created_at              │
└──────────────────────────┘
```

## Routing Structure

```
/
├── /login [GET, POST]
├── /signup [GET, POST]
├── /logout [GET]
├── /dashboard [GET] 🔒
├── /reports [GET, POST] 🔒
│   ├── /reports/create [POST]
│   ├── /reports/delete/{id} [POST]
│   └── /api/report/{id} [GET]
├── /upload [GET, POST] 🔒
├── /visualize [GET] 🔒
│   └── /api/report/{id}/details [GET]
└── /qr [GET, POST] 🔒
    ├── /qr/generate [POST]
    └── /qr/generate-bulk [POST]

🔒 = Protected (requires login)
```

## Technology Stack

```
Frontend:
├── HTML5
├── CSS3 (Gradients, Flexbox, Grid)
├── JavaScript (Vanilla, Fetch API)
└── Chart.js (Graphs)

Backend:
├── FastAPI
├── Uvicorn
├── SQLModel (ORM)
└── SessionMiddleware (Auth)

Services:
├── Google Cloud Vision (OCR)
├── YOLOv8 (Vehicle Detection)
├── AWS S3 (Image Storage)
├── qrcode (QR Generation)
└── reportlab (PDF Generation)

Database:
└── MySQL 8.0+

Security:
├── bcrypt (Password Hashing)
├── passlib (Password Utils)
├── Starlette Sessions (Auth)
└── CSRF Protection
```

## Data Flow Example: Complete Report Creation

```
1. User Action: Click "Create Report"
   ↓
2. Frontend: Display upload form
   ↓
3. User Action: Enter name + select images
   ↓
4. Frontend: POST /upload with files
   ↓
5. Backend: Create Report record in DB
   ↓
6. Backend: Save uploaded files to disk
   ↓
7. Backend: Queue background task (get_inferences)
   ↓
8. Return: "Upload successful, processing..."
   ↓
9. Background Job: For each image:
   ├─ Call Google OCR API
   ├─ Parse results (VIN, quantity, etc.)
   ├─ Detect vehicles with YOLO
   ├─ Upload image to S3
   ├─ Create Inference record in DB
   └─ Update report with results
   ↓
10. Frontend: Poll visualization page
    ↓
11. New images appear in real-time!
    ↓
12. User can view, search, and analyze data
```

## Security & Session Management

```
User Request
    ↓
Session Middleware
├─ Check session cookie
├─ Validate signature
└─ Load user data
    ↓
Route Handler
├─ Check if user in session
├─ If NO → Redirect to /login
└─ If YES → Process request
    ↓
Response
├─ Update session cookie
└─ Return response
```

---

**Architecture Last Updated:** December 11, 2025
**Status:** ✅ Complete and Functional
